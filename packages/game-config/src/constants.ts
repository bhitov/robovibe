/**
 * Constants for game configuration
 * Centralized configuration values used across the game engine
 */

// Tile and grid configuration
export const TILE_SIZE = 20; // Size of each tile in pixels

// Entity sizes
export const BOT_RADIUS = 8; // Radius of bots/players in pixels
export const ORB_RADIUS = 6; // Radius of orbs in pixels
export const BASE_RADIUS = 20; // Radius of bases in pixels
export const POWER_UP_RADIUS = 8; // Radius of power-ups in pixels